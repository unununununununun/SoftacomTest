program Test;

uses
  System.StartUpCopy,
  FMX.Forms,
  main in 'main.pas' {Form1},
  IntegrationService1 in 'IntegrationService1.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
